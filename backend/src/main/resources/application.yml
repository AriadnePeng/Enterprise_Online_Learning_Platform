server:
  port: ${SERVER_PORT:8080}

spring:
  application:
    name: enterprise-learning-backend
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/enterprise_learning_db?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&allowMultiQueries=true}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:@219JunfeiXiang}
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath*:mapper/*.xml
  configuration:
    map-underscore-to-camel-case: false
    call-setters-on-nulls: true
